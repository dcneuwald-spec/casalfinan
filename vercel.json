{
  "version": 2,
  "buildCommand": "cd backend && npm install --legacy-peer-deps && npx prisma generate && cd ../frontend && npm install --legacy-peer-deps && npm run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "functions": {
    "api/[...path].ts": {
      "maxDuration": 30
    }
  }
}
